import React from "react";
import { Quiz } from "../Types/JsonTypes";

const TypeBySound = ({ quiz, words }: { quiz: Quiz; words: string[] }) => {
  console.log(quiz);
  return <div className=''>sound</div>;
};

export default TypeBySound;
